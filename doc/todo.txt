TODO LIST：
1 内存存储结构调整。现在使用的是HASH方式，只支持对指定的属性进行快速搜索，也不支持范围搜索。
可能的方案：调整成列表+hash的存储方式，增加一定复杂性，可以提供更多的搜搜。或者使用mysql标准的B+数结构，优点是功能全面，缺点是实现逻辑复杂。
2 角色和用户的删除，现在是直接删除，需要增加一个校验，进行用户角色表的判断后才可以删除。但是这一点最好在内存存储结构调整后进行。
3 测试的使用方式，调整为使用pre方法预先准备好数据，然后才进行测试。
4 通过接口收到用户的密码后，对密码进行解密（简单方案：将第一个字符放到最后一个字符），然后再进行加密存放到自己的内存中（极简方案：将字符串逆序）。
在后续的所有使用中都先反逆序后再使用。该方法可以有多种实现，建议使用aes进行加密和解密。

